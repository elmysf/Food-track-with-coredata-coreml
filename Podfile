# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Food with coreml and coredata' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'lottie-ios'
  # Pods for Food with coreml and coredata

  target 'Food with coreml and coredataTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Food with coreml and coredataUITests' do
    # Pods for testing
  end

end
